# HANDOVER — מסע אומן

מסמך העברה. הגרסה הקודמת של המסמך תיארה קובץ HTML יחיד עם באג פתוח
בכניסה לציון; שניהם כבר לא רלוונטיים. מה שלהלן הוא המצב הנוכחי.

למידע על הרצה, מבנה הקבצים ועבודה עם הספרים והתצלומים — ראו
[README.md](README.md). המסמך הזה מתמקד במה שהשתנה ובמה שנשאר פתוח.

---

## 1. מה נעשה

### הקובץ פוצל
`uman-journey.html` (640KB, קובץ יחיד) פורק ל-`index.html` / `style.css` /
`game.js` / `sefaria.js` / `data/*.js` / `vendor/three.min.js`. זה היה
פריט 2 ב-backlog הקודם, ותנאי מקדים לכל השאר.

**המחיר:** המשחק כבר לא רץ מ-`file://`. התצלומים והסקריפטים נחסמים על ידי
CORS. צריך שרת סטטי — שורה אחת של `python3 -m http.server`.

### הבאג בכניסה לציון — נסגר
כל חמשת התסמינים מהמסמך הקודם, לפי הסדר:

| # | התסמין | מה נעשה |
|---|---|---|
| 1 | המסך קפץ ב-z≈-21.9, באמצע השביל | אין יותר popup אוטומטי. שלט עם כפתור ליד הסף, בדיוק כמו בספרייה |
| 2 | קיר בלתי נראה ב-z=-26, אי אפשר להגיע | גבול העולם הוזז ל-z=-55. הקיר בפועל הוא חזית המבנה ב-z=-28 |
| 3 | `blockBox` היה קוד מת | עכשיו הוא החוסם היחיד, ונבדק בבדיקת העשן |
| 4 | המשחק רץ מאחורי המסך המודאלי | `S.paused` עוטף את `updatePlayer` ואת `updateProximity` |
| 5 | `S.visitedTzion` נדלק פעם אחת | הוסר. השלט תלוי במרחק בלבד, ולכן חוזר על עצמו |

בנוסף נבנתה `sceneTzion` — פנים הציון — כסצנה שלישית מלאה (פריט 4
ב-backlog הקודם), לפי אותו דפוס של `goLibrary()`.

**באג נוסף שהתגלה תוך כדי:** `S.yaw` ההתחלתי היה `Math.PI`, כלומר השחקן
התחיל עם הגב לציון, וללכת "קדימה" הרחיק אותו ממנו. אותו דבר בכניסה
לספרייה. תוקן ל-0 בשני המקומות.

### תצלומים אמיתיים
15 תצלומים מוויקישיתוף — מבפנים ומבחוץ, מסוף המאה ה-19 ועד היום. פרטים
מלאים ב-[CREDITS.md](CREDITS.md) וב-README.

### הספרים נמשכים מספריא
`sefaria.js` מושך את הטקסטים מ-API של ספריא, עם עותק שמור ב-`localStorage`
וגיבוי מוטמע ב-`data/texts.js`. פריטים 3 ו-6 ב-backlog הקודם — הוצאת התוכן
מהקוד, והקראה של טקסט אמיתי.

### תיקונים קטנים מרשימת "בעיות ידועות"
- `updateProximity` כותב ל-DOM רק כשהטקסט משתנה (`setPlace` / `setPrompt`).
- `S.running` כבר לא נשאר `true` כשהקורא פתוח.
- טעינת קולות ה-TTS מנסה שוב ושוב במקום להסתמך על `onvoiceschanged`.
- התקדמות נשמרת ב-`localStorage` (פריט 7).

---

## 2. באג שכדאי להכיר: צבעים ב-three r128

הסצנות נראו שטופות ולבנות. הסיבה: ב-r128 ערכי `color` של חומרים אינם
מפוענחים מ-sRGB, בעוד שהטקסטורות כן (`encoding = sRGBEncoding`). התוצאה
היא שכל גוון כהה יוצא בהיר בהרבה מהכוונה, וכל משטח מואר נחתך ללבן.

התיקון: `col()` (שורה 96 ב-`game.js`) ממירה כל צבע חומר למרחב לינארי,
ו-`ACESFilmicToneMapping` מגלגל את הגבהים במקום לחתוך אותם.

**כל צבע חומר חדש חייב לעבור דרך `col()`.** אחרת החדר החדש ייראה שטוף
בדיוק כמו קודם. אורות (`PointLight` וכו׳) לא — שם העוצמה היא הפרמטר.

---

## 3. מפת העולם (יחידות = מטרים, עיניים בגובה 1.62)

**חוץ (`sceneOut`)** — התחלה `(0, 1.62, 34)`, `yaw = 0`, מבט אל `-Z`:
- שביל ראשי לאורך ציר Z, רוחב 7.
- **מבנה הציון:** מרכז `(0, -34)`, 14×11 → חזית `z = -28.5`.
  חסימה: `blockBox(0, -34, 7.4, 6.0)` → השחקן נעצר ב-`z = -28`.
  סף: `OUT.tzionDoor = (0, 0, -27.4)`, רדיוס השלט 4.4.
- **מבנה הספרייה:** מרכז `(-13, -12)`, 16×13 → חזית `z = -5.5`.
  חסימה: `blockBox(-13, -12, 8.4, 6.9)`. סף: `OUT.libDoor = (-13, 0, -4.6)`, רדיוס 3.6.
- גבול העולם: `x ∈ [-45, 45]`, `z ∈ [-55, 40]`.

**ציון (`sceneTz`)** — חדר 15×11, כניסה `(0, 1.62, 2.9)`, יציאה `z > 3.5`,
הציון על הקיר האחורי `z = -5.3`, שלט "לעמוד כאן רגע" ברדיוס 2.4 סביב `(0,0,-3.4)`.

**ספרייה (`sceneLib`)** — חדר 24×17, כניסה `(0, 1.62, 6.2)`, יציאה `z = 7.4`,
4 כווניות, 5 ספרים אינטראקטיביים ב-`interactives[]`.

---

## 4. מה נשאר פתוח

| חומרה | מה | איפה |
|---|---|---|
| בינונית | `speechSynthesis.pause()` לא נתמך בחלק מהאנדרואידים ונופל ל-`cancel()` — חידוש מתחיל את המשפט מחדש | `TTS.pause` |
| בינונית | אין סימון של המשפט המוקרא בתוך הטקסט. יש ל-CSS כלל `.said` מוכן, אבל אף אחד לא מוסיף אותו | `style.css`, `TTS.speakCurrent` |
| בינונית | `Likutei Moharan 282` הוא 12,000 תווים בעמוד אחד. שווה לפצל refs ארוכים לעמודי משנה | `data/books.js` |
| נמוכה | ההתנגשות בספרייה היא קלמפ מלבני בלבד; אפשר "להיכנס" לפינת שולחן הלימוד | `updatePlayer` |
| נמוכה | אין `dispose()` לטקסטורות ולגאומטריות — לא קריטי, הסצנות נבנות פעם אחת | — |
| נמוכה | אין אודיו חוץ מ-TTS: אין מוזיקת רקע, צעדים או צליל שליפת ספר | — |
| נמוכה | התרגומים לצרפתית נכתבו עבור המשחק ולא נבדקו מול מהדורה מוסמכת | `data/books.js` |

### Backlog מומלץ

1. סימון המשפט המוקרא (`boundary` event של `SpeechSynthesisUtterance`).
2. פיצול refs ארוכים לעמודי משנה, לפי `Likutei Moharan 282:1-8`.
3. שמע: מוזיקת רקע שקטה, צעדים, צליל שליפת ספר.
4. הקראה מוקלטת + `MediaSession API` במקום TTS — פותר גם את הנגינה ברקע.
5. עוד תצלומים. ויקישיתוף מחזיקה עוד חומר בקטגוריות
   `Pilgrimage to the tomb of Rabbi Nachman of Breslev` ו-`Kloyz of Breslev Chassidim in Uman`.

---

## 5. הערות תוכן

- הטקסטים העבריים בנחלת הכלל; מקורם ומהדורתם ב-`data/texts.js` ובדף
  שאליו מקשר כל עמוד. **המשחק אינו מהדורה מוסמכת.**
- התצלומים הם של מקום קדוש ושל אנשים בתפילה. הקרדיט המוצג בתוך המשחק הוא
  תנאי רישיון — לא להסיר אותו, ולא להסיר את `CREDITS.md`.
